#!/usr/bin/python3
"""a function that finds all multiples of 2 in a list"""


def divisible_by_2(my_list=[]):
    list_result = []
    for i in my_list:
        return list_result.append(my_list[i] % 2 == 0)

    return (list_result)
